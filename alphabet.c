#include <stdio.h>

int main()
{
    char ch;
    printf("Enter any character:");
    scanf("%c",&ch);
    if((ch>='a'&& ch<='z')||(ch>='A'&& ch<='Z'))
    printf("The enetered character %c is an Alphabet",ch);
    else
    printf("The enetered character %c is an Alphabet",ch);

    return 0;
}
